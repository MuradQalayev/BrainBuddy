package com.muradgalayev.brainbuddy

import org.junit.Test

import org.junit.Assert.*

// example local unit test, runs on the host machine
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}